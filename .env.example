# Ecco Studio — Local Development Environment
# Copy to `.env` and fill in values. Do NOT commit `.env`.

# Core
APP_ENV=dev
GCP_PROJECT=your-dev-project
GOOGLE_CLOUD_PROJECT=${GCP_PROJECT}
GCS_BUCKET=ecco-studio-platform-data
PORT=8080

# Local credentials (only for local dev; on GCP use ADC)
GOOGLE_APPLICATION_CREDENTIALS=

# Auth / API (Phase 14/15)
# Identity Platform / OIDC issuer and audiences for API and app
IDP_ISSUER=
IDP_AUDIENCE=
API_JWT_ISSUER=
API_JWT_AUDIENCE=

# Alerts / Webhooks (optional)
# If using webhook delivery (e.g., Slack), put the webhook URL here or in Secret Manager
ALERTS_WEBHOOK_URL=

# Integrations (optional)
JIRA_API_TOKEN=
ASANA_ACCESS_TOKEN=
ACCOUNTING_API_KEY=

# Terraform convenience (optional local overrides)
INVESTOR_RO_SERVICE_ACCOUNT=
ENABLE_CMEK=false
